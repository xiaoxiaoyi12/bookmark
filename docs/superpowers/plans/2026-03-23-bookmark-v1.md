# BookMark V1 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 构建一个 Web 端电子书阅读器，支持导入 EPUB/PDF，阅读时高亮标注，右侧做富文本笔记。

**架构：** React 单页应用，两个主视图（书库 + 阅读器）。阅读器采用三栏布局：目录 | 阅读区 | 笔记。所有数据通过 Dexie.js 存储在本地 IndexedDB，无需后端。

**技术栈：** React 18, TypeScript, Vite, Tailwind CSS, epub.js, pdfjs-dist, Tiptap, Dexie.js, Zustand, React Router

**设计文档：** `docs/superpowers/specs/2026-03-23-bookmark-v1-design.md`

---

## 文件结构

```
src/
├── main.tsx                       # 应用入口
├── App.tsx                        # 路由配置（书库 / 阅读器）
├── db/
│   └── index.ts                   # Dexie 数据库定义
├── stores/
│   └── useReaderStore.ts          # Zustand 状态管理（面板开关等）
├── types/
│   └── index.ts                   # TypeScript 类型定义（Book, Highlight, Note）
├── pages/
│   ├── LibraryPage.tsx            # 书库页面（网格视图）
│   └── ReaderPage.tsx             # 阅读页面（三栏布局）
├── components/
│   ├── library/
│   │   ├── BookCard.tsx           # 书籍卡片（封面、标题、进度）
│   │   ├── BookGrid.tsx           # 书籍网格
│   │   └── ImportDropzone.tsx     # 拖拽导入区域
│   ├── reader/
│   │   ├── EpubReader.tsx         # epub.js 渲染组件
│   │   ├── PdfReader.tsx          # PDF.js 渲染组件
│   │   ├── TableOfContents.tsx    # 左侧目录面板
│   │   └── SelectionToolbar.tsx   # 选中文字后的浮动工具栏
│   └── notes/
│       ├── NotePanel.tsx          # 右侧笔记面板
│       ├── TiptapEditor.tsx       # Tiptap 富文本编辑器
│       └── HighlightList.tsx      # 高亮列表
└── utils/
    └── epub-metadata.ts           # 从 EPUB 提取元数据和封面
```

---

## 任务 1：项目脚手架

**文件：**
- 创建：所有配置文件、`src/main.tsx`、`src/App.tsx`

- [ ] **步骤 1：创建 Vite + React + TypeScript 项目**

```bash
cd ~/Desktop
npm create vite@latest bookmark -- --template react-ts
cd bookmark
```

- [ ] **步骤 2：安装核心依赖**

```bash
npm install react-router-dom dexie zustand epubjs pdfjs-dist @tiptap/react @tiptap/starter-kit @tiptap/extension-placeholder
npm install -D tailwindcss @tailwindcss/vite
```

- [ ] **步骤 3：配置 Tailwind CSS**

更新 `src/index.css`：
```css
@import "tailwindcss";
```

更新 `vite.config.ts`：
```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

- [ ] **步骤 4：配置 React Router**

```tsx
// src/App.tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import LibraryPage from './pages/LibraryPage'
import ReaderPage from './pages/ReaderPage'

export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<LibraryPage />} />
        <Route path="/read/:bookId" element={<ReaderPage />} />
      </Routes>
    </BrowserRouter>
  )
}
```

创建占位页面：

```tsx
// src/pages/LibraryPage.tsx
export default function LibraryPage() {
  return <div className="p-8 text-white">BookMark 书库</div>
}
```

```tsx
// src/pages/ReaderPage.tsx
export default function ReaderPage() {
  return <div className="p-8 text-white">阅读器</div>
}
```

- [ ] **步骤 5：验证开发服务器启动**

```bash
npm run dev
```

预期：应用在 localhost 运行，`/` 显示"BookMark 书库"，`/read/test` 显示"阅读器"。

- [ ] **步骤 6：初始化 git 并提交**

```bash
cd ~/Desktop/bookmark
git init
git add -A
git commit -m "chore: 初始化项目 Vite + React + TypeScript + Tailwind"
```

---

## 任务 2：数据库与类型定义

**文件：**
- 创建：`src/types/index.ts`、`src/db/index.ts`

- [ ] **步骤 1：定义 TypeScript 类型**

```ts
// src/types/index.ts
export interface Book {
  id?: number
  title: string
  author: string
  format: 'epub' | 'pdf'
  coverUrl?: string          // base64 数据 URL
  fileData: ArrayBuffer       // 文件原始字节
  createdAt: number           // Date.now()
}

export interface Highlight {
  id?: number
  bookId: number
  cfiRange: string            // EPUB CFI 或 PDF "page:N:rect"
  text: string                // 高亮文字内容
  color: string               // 颜色值
  createdAt: number
}

export interface Note {
  id?: number
  bookId: number
  content: string             // Tiptap JSON 序列化字符串
  updatedAt: number
}

export interface ReadingProgress {
  bookId: number              // 主键，每本书一条记录
  location: string            // EPUB CFI 或 PDF 页码
  updatedAt: number
}
```

- [ ] **步骤 2：创建 Dexie 数据库**

```ts
// src/db/index.ts
import Dexie, { type Table } from 'dexie'
import type { Book, Highlight, Note, ReadingProgress } from '../types'

class BookMarkDB extends Dexie {
  books!: Table<Book, number>
  highlights!: Table<Highlight, number>
  notes!: Table<Note, number>
  readingProgress!: Table<ReadingProgress, number>

  constructor() {
    super('bookmark-db')
    this.version(1).stores({
      books: '++id, title, format, createdAt',
      highlights: '++id, bookId, createdAt',
      notes: '++id, bookId',
      readingProgress: 'bookId',
    })
  }
}

export const db = new BookMarkDB()
```

- [ ] **步骤 3：验证数据库初始化**

在 `LibraryPage.tsx` 中添加临时测试：

```tsx
import { useEffect } from 'react'
import { db } from '../db'

export default function LibraryPage() {
  useEffect(() => {
    db.books.count().then(c => console.log('书籍数量:', c))
  }, [])
  return <div className="p-8 text-white">BookMark 书库</div>
}
```

打开浏览器 DevTools → Application → IndexedDB → 确认 `bookmark-db` 存在且有 4 个表。

- [ ] **步骤 4：提交**

```bash
git add src/types/index.ts src/db/index.ts src/pages/LibraryPage.tsx
git commit -m "feat: 添加数据库定义和 TypeScript 类型"
```

---

## 任务 3：书籍导入与书库页面

**文件：**
- 创建：`src/components/library/ImportDropzone.tsx`、`src/components/library/BookCard.tsx`、`src/components/library/BookGrid.tsx`、`src/utils/epub-metadata.ts`
- 修改：`src/pages/LibraryPage.tsx`

- [ ] **步骤 1：创建 EPUB 元数据提取工具**

```ts
// src/utils/epub-metadata.ts
import ePub from 'epubjs'

export async function extractEpubMetadata(data: ArrayBuffer) {
  const book = ePub(data)
  await book.ready

  const metadata = await book.loaded.metadata
  let coverUrl: string | undefined

  try {
    const coverUrl_ = await book.coverUrl()
    if (coverUrl_) coverUrl = coverUrl_
  } catch {
    // 无封面
  }

  book.destroy()

  return {
    title: metadata.title || '未命名',
    author: metadata.creator || '未知作者',
    coverUrl,
  }
}
```

- [ ] **步骤 2：创建 ImportDropzone 组件**

```tsx
// src/components/library/ImportDropzone.tsx
import { useCallback } from 'react'
import { db } from '../../db'
import { extractEpubMetadata } from '../../utils/epub-metadata'
import type { Book } from '../../types'

interface Props {
  onImported: () => void
}

export default function ImportDropzone({ onImported }: Props) {
  const handleFiles = useCallback(async (files: FileList | null) => {
    if (!files) return

    for (const file of Array.from(files)) {
      const ext = file.name.split('.').pop()?.toLowerCase()
      if (ext !== 'epub' && ext !== 'pdf') continue

      const data = await file.arrayBuffer()
      const format = ext as 'epub' | 'pdf'

      let title = file.name.replace(/\.[^.]+$/, '')
      let author = '未知作者'
      let coverUrl: string | undefined

      if (format === 'epub') {
        const meta = await extractEpubMetadata(data)
        title = meta.title
        author = meta.author
        coverUrl = meta.coverUrl
      }

      const book: Book = {
        title,
        author,
        format,
        coverUrl,
        fileData: data,
        createdAt: Date.now(),
      }

      await db.books.add(book)
    }

    onImported()
  }, [onImported])

  return (
    <div
      className="border-2 border-dashed border-gray-600 rounded-lg p-8 text-center cursor-pointer hover:border-gray-400 transition-colors"
      onDragOver={e => e.preventDefault()}
      onDrop={e => { e.preventDefault(); handleFiles(e.dataTransfer.files) }}
      onClick={() => {
        const input = document.createElement('input')
        input.type = 'file'
        input.accept = '.epub,.pdf'
        input.multiple = true
        input.onchange = () => handleFiles(input.files)
        input.click()
      }}
    >
      <p className="text-gray-400 text-lg">拖拽 EPUB / PDF 文件到这里，或点击导入</p>
    </div>
  )
}
```

- [ ] **步骤 3：创建 BookCard 组件**

```tsx
// src/components/library/BookCard.tsx
import { useNavigate } from 'react-router-dom'
import type { Book } from '../../types'
import { db } from '../../db'

interface Props {
  book: Book
  onDeleted: () => void
}

export default function BookCard({ book, onDeleted }: Props) {
  const navigate = useNavigate()

  const handleDelete = async (e: React.MouseEvent) => {
    e.stopPropagation()
    if (!book.id) return
    await db.books.delete(book.id)
    await db.highlights.where('bookId').equals(book.id).delete()
    await db.notes.where('bookId').equals(book.id).delete()
    await db.readingProgress.delete(book.id)
    onDeleted()
  }

  return (
    <div
      className="group relative bg-gray-800 rounded-lg overflow-hidden cursor-pointer hover:ring-2 hover:ring-blue-500 transition-all"
      onClick={() => navigate(`/read/${book.id}`)}
    >
      <div className="aspect-[3/4] bg-gray-700 flex items-center justify-center">
        {book.coverUrl ? (
          <img src={book.coverUrl} alt={book.title} className="w-full h-full object-cover" />
        ) : (
          <span className="text-4xl text-gray-500">📖</span>
        )}
      </div>
      <div className="p-3">
        <h3 className="text-sm font-medium text-white truncate">{book.title}</h3>
        <p className="text-xs text-gray-400 truncate">{book.author}</p>
        <span className="text-xs text-gray-500 uppercase">{book.format}</span>
      </div>
      <button
        onClick={handleDelete}
        className="absolute top-2 right-2 hidden group-hover:block bg-red-600 text-white rounded-full w-6 h-6 text-xs leading-none"
      >
        ✕
      </button>
    </div>
  )
}
```

- [ ] **步骤 4：创建 BookGrid 组件**

```tsx
// src/components/library/BookGrid.tsx
import type { Book } from '../../types'
import BookCard from './BookCard'

interface Props {
  books: Book[]
  onChanged: () => void
}

export default function BookGrid({ books, onChanged }: Props) {
  if (books.length === 0) {
    return <p className="text-gray-500 text-center py-12">还没有书籍，导入一本开始吧。</p>
  }

  return (
    <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4">
      {books.map(book => (
        <BookCard key={book.id} book={book} onDeleted={onChanged} />
      ))}
    </div>
  )
}
```

- [ ] **步骤 5：组装 LibraryPage**

```tsx
// src/pages/LibraryPage.tsx
import { useState, useEffect, useCallback } from 'react'
import { db } from '../db'
import type { Book } from '../types'
import ImportDropzone from '../components/library/ImportDropzone'
import BookGrid from '../components/library/BookGrid'

export default function LibraryPage() {
  const [books, setBooks] = useState<Book[]>([])

  const loadBooks = useCallback(async () => {
    const all = await db.books.orderBy('createdAt').reverse().toArray()
    setBooks(all)
  }, [])

  useEffect(() => { loadBooks() }, [loadBooks])

  return (
    <div className="min-h-screen bg-gray-900 p-8">
      <h1 className="text-2xl font-bold text-white mb-6">BookMark</h1>
      <ImportDropzone onImported={loadBooks} />
      <div className="mt-8">
        <BookGrid books={books} onChanged={loadBooks} />
      </div>
    </div>
  )
}
```

- [ ] **步骤 6：手动测试**

1. 运行 `npm run dev`
2. 拖拽一个 EPUB 文件到导入区域 → 确认书籍显示在网格中（封面、标题、作者）
3. 拖拽一个 PDF 文件 → 确认显示（无封面，显示文件名作为标题）
4. 点击删除按钮 → 确认书籍被移除
5. 刷新页面 → 确认书籍数据持久化

- [ ] **步骤 7：提交**

```bash
git add src/
git commit -m "feat: 书籍导入和书库页面"
```

---

## 任务 4：EPUB 阅读器

**文件：**
- 创建：`src/components/reader/EpubReader.tsx`、`src/components/reader/TableOfContents.tsx`、`src/stores/useReaderStore.ts`
- 修改：`src/pages/ReaderPage.tsx`

- [ ] **步骤 1：创建 Zustand 状态管理**

```ts
// src/stores/useReaderStore.ts
import { create } from 'zustand'

interface ReaderState {
  tocOpen: boolean
  noteOpen: boolean
  toggleToc: () => void
  toggleNote: () => void
}

export const useReaderStore = create<ReaderState>((set) => ({
  tocOpen: true,
  noteOpen: true,
  toggleToc: () => set(s => ({ tocOpen: !s.tocOpen })),
  toggleNote: () => set(s => ({ noteOpen: !s.noteOpen })),
}))
```

- [ ] **步骤 2：创建目录组件**

```tsx
// src/components/reader/TableOfContents.tsx
import type { NavItem } from 'epubjs'

interface Props {
  items: NavItem[]
  onSelect: (href: string) => void
}

export default function TableOfContents({ items, onSelect }: Props) {
  const renderItems = (navItems: NavItem[], depth = 0) => (
    <ul>
      {navItems.map((item, i) => (
        <li key={i}>
          <button
            className="w-full text-left px-3 py-1.5 text-sm text-gray-300 hover:bg-gray-700 rounded truncate"
            style={{ paddingLeft: `${12 + depth * 16}px` }}
            onClick={() => onSelect(item.href)}
          >
            {item.label.trim()}
          </button>
          {item.subitems && item.subitems.length > 0 && renderItems(item.subitems, depth + 1)}
        </li>
      ))}
    </ul>
  )

  return (
    <nav className="h-full overflow-y-auto py-2">
      <h2 className="px-3 py-2 text-xs font-semibold text-gray-500 uppercase">目录</h2>
      {renderItems(items)}
    </nav>
  )
}
```

- [ ] **步骤 3：创建 EpubReader 组件**

```tsx
// src/components/reader/EpubReader.tsx
import { useEffect, useRef, useState } from 'react'
import ePub, { type Rendition, type NavItem } from 'epubjs'
import { db } from '../../db'
import TableOfContents from './TableOfContents'
import { useReaderStore } from '../../stores/useReaderStore'

interface Props {
  bookId: number
  fileData: ArrayBuffer
}

export default function EpubReader({ bookId, fileData }: Props) {
  const viewerRef = useRef<HTMLDivElement>(null)
  const renditionRef = useRef<Rendition | null>(null)
  const [toc, setToc] = useState<NavItem[]>([])
  const { tocOpen } = useReaderStore()

  useEffect(() => {
    if (!viewerRef.current) return

    const book = ePub(fileData)
    const rendition = book.renderTo(viewerRef.current, {
      width: '100%',
      height: '100%',
      spread: 'none',
    })

    renditionRef.current = rendition

    // 加载保存的阅读进度，或从头开始
    db.readingProgress.get(bookId).then(progress => {
      if (progress?.location) {
        rendition.display(progress.location)
      } else {
        rendition.display()
      }
    })

    // 加载目录
    book.loaded.navigation.then(nav => setToc(nav.toc))

    // 翻页时保存进度
    rendition.on('relocated', (location: { start: { cfi: string } }) => {
      db.readingProgress.put({
        bookId,
        location: location.start.cfi,
        updatedAt: Date.now(),
      })
    })

    // 键盘翻页
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'ArrowRight' || e.key === 'ArrowDown') rendition.next()
      if (e.key === 'ArrowLeft' || e.key === 'ArrowUp') rendition.prev()
    }
    document.addEventListener('keydown', handleKeyDown)

    return () => {
      document.removeEventListener('keydown', handleKeyDown)
      rendition.destroy()
      book.destroy()
    }
  }, [bookId, fileData])

  const handleTocSelect = (href: string) => {
    renditionRef.current?.display(href)
  }

  return (
    <div className="flex h-full">
      {tocOpen && (
        <div className="w-60 border-r border-gray-700 shrink-0">
          <TableOfContents items={toc} onSelect={handleTocSelect} />
        </div>
      )}
      <div className="flex-1 min-w-0">
        <div ref={viewerRef} className="h-full" />
      </div>
    </div>
  )
}
```

- [ ] **步骤 4：组装阅读页面**

```tsx
// src/pages/ReaderPage.tsx
import { useState, useEffect } from 'react'
import { useParams, useNavigate } from 'react-router-dom'
import { db } from '../db'
import type { Book } from '../types'
import { useReaderStore } from '../stores/useReaderStore'
import EpubReader from '../components/reader/EpubReader'

export default function ReaderPage() {
  const { bookId } = useParams<{ bookId: string }>()
  const navigate = useNavigate()
  const [book, setBook] = useState<Book | null>(null)
  const { toggleToc, toggleNote } = useReaderStore()

  useEffect(() => {
    if (!bookId) return
    db.books.get(Number(bookId)).then(b => {
      if (!b) navigate('/')
      else setBook(b)
    })
  }, [bookId, navigate])

  if (!book) return <div className="min-h-screen bg-gray-900 flex items-center justify-center text-gray-400">加载中...</div>

  return (
    <div className="h-screen bg-gray-900 flex flex-col">
      {/* 顶部工具栏 */}
      <div className="h-12 border-b border-gray-700 flex items-center px-4 shrink-0">
        <button onClick={() => navigate('/')} className="text-gray-400 hover:text-white text-sm mr-4">← 书库</button>
        <h1 className="text-sm text-white font-medium truncate flex-1">{book.title}</h1>
        <button onClick={toggleToc} className="text-gray-400 hover:text-white text-sm mx-2">目录</button>
        <button onClick={toggleNote} className="text-gray-400 hover:text-white text-sm">笔记</button>
      </div>

      {/* 阅读区域 */}
      <div className="flex-1 min-h-0">
        {book.format === 'epub' && book.id && (
          <EpubReader bookId={book.id} fileData={book.fileData} />
        )}
        {book.format === 'pdf' && (
          <div className="flex items-center justify-center h-full text-gray-500">PDF 阅读器即将到来...</div>
        )}
      </div>
    </div>
  )
}
```

- [ ] **步骤 5：手动测试**

1. 从书库导入一个 EPUB 文件
2. 点击 → 确认在阅读器中打开
3. 确认左侧目录显示，章节可点击跳转
4. 方向键翻页
5. 关闭后重新打开 → 确认阅读进度恢复
6. 点击"← 书库" → 返回书库

- [ ] **步骤 6：提交**

```bash
git add src/
git commit -m "feat: EPUB 阅读器，支持目录导航和进度保存"
```

---

## 任务 5：PDF 阅读器

**文件：**
- 创建：`src/components/reader/PdfReader.tsx`
- 修改：`src/pages/ReaderPage.tsx`、`vite.config.ts`

- [ ] **步骤 1：配置 PDF.js Worker**

更新 `vite.config.ts`：
```ts
export default defineConfig({
  plugins: [react(), tailwindcss()],
  optimizeDeps: {
    include: ['pdfjs-dist'],
  },
})
```

- [ ] **步骤 2：创建 PdfReader 组件**

```tsx
// src/components/reader/PdfReader.tsx
import { useEffect, useRef, useState } from 'react'
import * as pdfjsLib from 'pdfjs-dist'
import { db } from '../../db'

pdfjsLib.GlobalWorkerOptions.workerSrc = new URL(
  'pdfjs-dist/build/pdf.worker.mjs',
  import.meta.url,
).toString()

interface Props {
  bookId: number
  fileData: ArrayBuffer
}

export default function PdfReader({ bookId, fileData }: Props) {
  const canvasRef = useRef<HTMLCanvasElement>(null)
  const [pdfDoc, setPdfDoc] = useState<pdfjsLib.PDFDocumentProxy | null>(null)
  const [currentPage, setCurrentPage] = useState(1)
  const [totalPages, setTotalPages] = useState(0)

  // 加载 PDF 文档
  useEffect(() => {
    const load = async () => {
      const doc = await pdfjsLib.getDocument({ data: fileData.slice(0) }).promise
      setPdfDoc(doc)
      setTotalPages(doc.numPages)

      // 恢复阅读进度
      const progress = await db.readingProgress.get(bookId)
      if (progress?.location) {
        setCurrentPage(Number(progress.location))
      }
    }
    load()
  }, [bookId, fileData])

  // 渲染当前页
  useEffect(() => {
    if (!pdfDoc || !canvasRef.current) return

    const render = async () => {
      const page = await pdfDoc.getPage(currentPage)
      const viewport = page.getViewport({ scale: 1.5 })
      const canvas = canvasRef.current!
      canvas.width = viewport.width
      canvas.height = viewport.height

      await page.render({
        canvasContext: canvas.getContext('2d')!,
        viewport,
      }).promise
    }
    render()

    // 保存进度
    db.readingProgress.put({
      bookId,
      location: String(currentPage),
      updatedAt: Date.now(),
    })
  }, [pdfDoc, currentPage, bookId])

  // 键盘翻页
  useEffect(() => {
    const handleKey = (e: KeyboardEvent) => {
      if (e.key === 'ArrowRight' || e.key === 'ArrowDown') {
        setCurrentPage(p => Math.min(p + 1, totalPages))
      }
      if (e.key === 'ArrowLeft' || e.key === 'ArrowUp') {
        setCurrentPage(p => Math.max(p - 1, 1))
      }
    }
    document.addEventListener('keydown', handleKey)
    return () => document.removeEventListener('keydown', handleKey)
  }, [totalPages])

  return (
    <div className="h-full flex flex-col items-center">
      {/* 翻页控制 */}
      <div className="flex items-center gap-3 py-2 shrink-0">
        <button
          onClick={() => setCurrentPage(p => Math.max(p - 1, 1))}
          disabled={currentPage <= 1}
          className="text-gray-400 hover:text-white disabled:opacity-30 text-sm"
        >
          ← 上一页
        </button>
        <span className="text-gray-400 text-sm">{currentPage} / {totalPages}</span>
        <button
          onClick={() => setCurrentPage(p => Math.min(p + 1, totalPages))}
          disabled={currentPage >= totalPages}
          className="text-gray-400 hover:text-white disabled:opacity-30 text-sm"
        >
          下一页 →
        </button>
      </div>

      {/* PDF 画布 */}
      <div className="flex-1 overflow-auto flex justify-center">
        <canvas ref={canvasRef} />
      </div>
    </div>
  )
}
```

- [ ] **步骤 3：接入 ReaderPage**

在 `src/pages/ReaderPage.tsx` 中替换 PDF 占位符：

```tsx
// 顶部添加导入
import PdfReader from '../components/reader/PdfReader'

// 替换 JSX 中的 PDF 占位符：
{book.format === 'pdf' && book.id && (
  <PdfReader bookId={book.id} fileData={book.fileData} />
)}
```

- [ ] **步骤 4：手动测试**

1. 导入一个 PDF 文件
2. 点击 → 确认页面正常渲染
3. 方向键/按钮翻页
4. 关闭后重新打开 → 确认页码恢复

- [ ] **步骤 5：提交**

```bash
git add src/
git commit -m "feat: PDF 阅读器，支持翻页和进度保存"
```

---

## 任务 6：笔记面板（Tiptap 编辑器）

**文件：**
- 创建：`src/components/notes/TiptapEditor.tsx`、`src/components/notes/NotePanel.tsx`
- 修改：`src/pages/ReaderPage.tsx`

- [ ] **步骤 1：创建 TiptapEditor 组件**

```tsx
// src/components/notes/TiptapEditor.tsx
import { useEffect, useRef } from 'react'
import { useEditor, EditorContent } from '@tiptap/react'
import StarterKit from '@tiptap/starter-kit'
import Placeholder from '@tiptap/extension-placeholder'

interface Props {
  content: string    // JSON 字符串
  onUpdate: (json: string) => void
}

export default function TiptapEditor({ content, onUpdate }: Props) {
  const initialContent = useRef(content)

  const editor = useEditor({
    extensions: [
      StarterKit,
      Placeholder.configure({ placeholder: '开始写笔记...' }),
    ],
    content: initialContent.current ? JSON.parse(initialContent.current) : undefined,
    onUpdate: ({ editor }) => {
      onUpdate(JSON.stringify(editor.getJSON()))
    },
  })

  // 暴露编辑器实例，供外部插入高亮引用
  useEffect(() => {
    if (editor) {
      (window as any).__tiptapEditor = editor
    }
    return () => { (window as any).__tiptapEditor = undefined }
  }, [editor])

  if (!editor) return null

  return (
    <div className="flex flex-col h-full">
      {/* 工具栏 */}
      <div className="flex gap-1 p-2 border-b border-gray-700 shrink-0 flex-wrap">
        <button onClick={() => editor.chain().focus().toggleHeading({ level: 2 }).run()}
          className={`px-2 py-1 text-xs rounded ${editor.isActive('heading', { level: 2 }) ? 'bg-gray-600 text-white' : 'text-gray-400 hover:bg-gray-700'}`}>
          H2
        </button>
        <button onClick={() => editor.chain().focus().toggleBold().run()}
          className={`px-2 py-1 text-xs rounded ${editor.isActive('bold') ? 'bg-gray-600 text-white' : 'text-gray-400 hover:bg-gray-700'}`}>
          B
        </button>
        <button onClick={() => editor.chain().focus().toggleItalic().run()}
          className={`px-2 py-1 text-xs rounded ${editor.isActive('italic') ? 'bg-gray-600 text-white' : 'text-gray-400 hover:bg-gray-700'}`}>
          I
        </button>
        <button onClick={() => editor.chain().focus().toggleBulletList().run()}
          className={`px-2 py-1 text-xs rounded ${editor.isActive('bulletList') ? 'bg-gray-600 text-white' : 'text-gray-400 hover:bg-gray-700'}`}>
          列表
        </button>
        <button onClick={() => editor.chain().focus().toggleBlockquote().run()}
          className={`px-2 py-1 text-xs rounded ${editor.isActive('blockquote') ? 'bg-gray-600 text-white' : 'text-gray-400 hover:bg-gray-700'}`}>
          引用
        </button>
        <button onClick={() => editor.chain().focus().toggleCodeBlock().run()}
          className={`px-2 py-1 text-xs rounded ${editor.isActive('codeBlock') ? 'bg-gray-600 text-white' : 'text-gray-400 hover:bg-gray-700'}`}>
          代码
        </button>
      </div>

      {/* 编辑器 */}
      <EditorContent
        editor={editor}
        className="flex-1 overflow-y-auto p-4 prose prose-invert prose-sm max-w-none"
      />
    </div>
  )
}
```

- [ ] **步骤 2：创建 NotePanel 组件**

```tsx
// src/components/notes/NotePanel.tsx
import { useState, useEffect, useCallback } from 'react'
import { db } from '../../db'
import type { Note } from '../../types'
import TiptapEditor from './TiptapEditor'

interface Props {
  bookId: number
}

export default function NotePanel({ bookId }: Props) {
  const [note, setNote] = useState<Note | null>(null)
  const [loaded, setLoaded] = useState(false)

  useEffect(() => {
    db.notes.where('bookId').equals(bookId).first().then(n => {
      setNote(n || null)
      setLoaded(true)
    })
  }, [bookId])

  const handleUpdate = useCallback(async (content: string) => {
    if (note?.id) {
      await db.notes.update(note.id, { content, updatedAt: Date.now() })
    } else {
      const id = await db.notes.add({ bookId, content, updatedAt: Date.now() })
      setNote({ id, bookId, content, updatedAt: Date.now() })
    }
  }, [bookId, note?.id])

  if (!loaded) return null

  return (
    <div className="h-full flex flex-col">
      <h2 className="px-4 py-2 text-xs font-semibold text-gray-500 uppercase border-b border-gray-700 shrink-0">
        笔记
      </h2>
      <TiptapEditor content={note?.content || ''} onUpdate={handleUpdate} />
    </div>
  )
}
```

- [ ] **步骤 3：集成到 ReaderPage**

更新 `src/pages/ReaderPage.tsx`，添加右侧面板：

```tsx
// 添加导入
import NotePanel from '../components/notes/NotePanel'

// 阅读区域改为 flex 布局：
<div className="flex-1 min-h-0 flex">
  <div className="flex-1 min-w-0">
    {book.format === 'epub' && book.id && (
      <EpubReader bookId={book.id} fileData={book.fileData} />
    )}
    {book.format === 'pdf' && book.id && (
      <PdfReader bookId={book.id} fileData={book.fileData} />
    )}
  </div>

  {noteOpen && book.id && (
    <div className="w-[360px] border-l border-gray-700 shrink-0">
      <NotePanel bookId={book.id} />
    </div>
  )}
</div>
```

（解构中添加 `const { tocOpen, noteOpen, toggleToc, toggleNote } = useReaderStore()`）

- [ ] **步骤 4：手动测试**

1. 打开一本书 → 确认右侧笔记面板显示
2. 在编辑器中输入 → 加粗、标题、列表、引用都能正常工作
3. 关闭后重新打开 → 确认笔记内容持久化
4. 点击"笔记"按钮 → 面板隐藏/显示

- [ ] **步骤 5：提交**

```bash
git add src/
git commit -m "feat: 笔记面板，Tiptap 富文本编辑器"
```

---

## 任务 7：文字选中、高亮与引用到笔记

**文件：**
- 创建：`src/components/reader/SelectionToolbar.tsx`、`src/components/notes/HighlightList.tsx`
- 修改：`src/components/reader/EpubReader.tsx`、`src/components/notes/NotePanel.tsx`

- [ ] **步骤 1：创建 SelectionToolbar 组件**

```tsx
// src/components/reader/SelectionToolbar.tsx
import { useState } from 'react'

const COLORS = ['#FBBF24', '#34D399', '#60A5FA', '#F87171', '#C084FC']

interface Props {
  position: { x: number; y: number }
  onHighlight: (color: string) => void
  onAddToNote: (color: string) => void
  onClose: () => void
}

export default function SelectionToolbar({ position, onHighlight, onAddToNote, onClose }: Props) {
  const [showColors, setShowColors] = useState(false)
  const [action, setAction] = useState<'highlight' | 'note' | null>(null)

  const handleColorPick = (color: string) => {
    if (action === 'highlight') onHighlight(color)
    if (action === 'note') onAddToNote(color)
    setShowColors(false)
    setAction(null)
    onClose()
  }

  return (
    <div
      className="fixed z-50 bg-gray-800 rounded-lg shadow-xl border border-gray-600 p-1 flex gap-1"
      style={{ left: position.x, top: position.y }}
    >
      {!showColors ? (
        <>
          <button
            className="px-3 py-1.5 text-sm text-gray-200 hover:bg-gray-700 rounded"
            onClick={() => { setAction('highlight'); setShowColors(true) }}
          >
            高亮
          </button>
          <button
            className="px-3 py-1.5 text-sm text-gray-200 hover:bg-gray-700 rounded"
            onClick={() => { setAction('note'); setShowColors(true) }}
          >
            添加到笔记
          </button>
          <button
            className="px-3 py-1.5 text-sm text-gray-200 hover:bg-gray-700 rounded"
            onClick={() => { navigator.clipboard.writeText(window.getSelection()?.toString() || ''); onClose() }}
          >
            复制
          </button>
        </>
      ) : (
        <div className="flex gap-1 p-1">
          {COLORS.map(c => (
            <button
              key={c}
              className="w-6 h-6 rounded-full border-2 border-transparent hover:border-white"
              style={{ backgroundColor: c }}
              onClick={() => handleColorPick(c)}
            />
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **步骤 2：在 EpubReader 中添加文字选中处理**

在 `EpubReader.tsx` 的 useEffect 中，`rendition.on('relocated', ...)` 之后添加选中监听：

```tsx
// 添加 state
const [selectionData, setSelectionData] = useState<{
  text: string; cfiRange: string; position: { x: number; y: number }
} | null>(null)

// useEffect 内添加：
rendition.on('selected', (cfiRange: string) => {
  const selection = rendition.manager?.container?.ownerDocument?.getSelection()
  if (!selection || selection.isCollapsed) return

  const text = selection.toString().trim()
  if (!text) return

  const range = selection.getRangeAt(0)
  const rect = range.getBoundingClientRect()

  setSelectionData({
    text,
    cfiRange,
    position: { x: rect.left + rect.width / 2 - 80, y: rect.bottom + 8 },
  })
})

// 添加高亮处理函数
const handleHighlight = async (color: string) => {
  if (!selectionData) return
  await db.highlights.add({
    bookId,
    cfiRange: selectionData.cfiRange,
    text: selectionData.text,
    color,
    createdAt: Date.now(),
  })
  renditionRef.current?.annotations.highlight(
    selectionData.cfiRange, {}, () => {}, '', { fill: color, 'fill-opacity': '0.3' }
  )
  setSelectionData(null)
}

const handleAddToNote = async (color: string) => {
  await handleHighlight(color)
  const editor = (window as any).__tiptapEditor
  if (editor && selectionData) {
    editor.chain().focus().insertContent([
      { type: 'blockquote', content: [{ type: 'paragraph', content: [{ type: 'text', text: selectionData.text }] }] },
      { type: 'paragraph' },
    ]).run()
  }
}

// JSX 中添加工具栏：
{selectionData && (
  <SelectionToolbar
    position={selectionData.position}
    onHighlight={handleHighlight}
    onAddToNote={handleAddToNote}
    onClose={() => setSelectionData(null)}
  />
)}
```

- [ ] **步骤 3：打开书籍时恢复高亮**

在 EpubReader 的 useEffect 中，`rendition.display()` 之后添加：

```tsx
// 加载并应用已保存的高亮
db.highlights.where('bookId').equals(bookId).toArray().then(highlights => {
  highlights.forEach(h => {
    rendition.annotations.highlight(
      h.cfiRange, {}, () => {}, '', { fill: h.color, 'fill-opacity': '0.3' }
    )
  })
})
```

- [ ] **步骤 4：创建 HighlightList 组件**

```tsx
// src/components/notes/HighlightList.tsx
import { useState, useEffect } from 'react'
import { db } from '../../db'
import type { Highlight } from '../../types'

interface Props {
  bookId: number
}

export default function HighlightList({ bookId }: Props) {
  const [highlights, setHighlights] = useState<Highlight[]>([])

  useEffect(() => {
    db.highlights.where('bookId').equals(bookId)
      .reverse().sortBy('createdAt')
      .then(setHighlights)
  }, [bookId])

  if (highlights.length === 0) return null

  return (
    <div className="border-t border-gray-700">
      <h3 className="px-4 py-2 text-xs font-semibold text-gray-500 uppercase">
        高亮 ({highlights.length})
      </h3>
      <div className="max-h-60 overflow-y-auto">
        {highlights.map(h => (
          <div key={h.id} className="px-4 py-2 border-b border-gray-800">
            <div className="flex items-start gap-2">
              <div className="w-2 h-2 rounded-full mt-1.5 shrink-0" style={{ backgroundColor: h.color }} />
              <p className="text-sm text-gray-300 line-clamp-3">{h.text}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **步骤 5：在 NotePanel 中添加 HighlightList**

在 `NotePanel.tsx` 中，TiptapEditor 下方添加：

```tsx
import HighlightList from './HighlightList'

// JSX 中：
<TiptapEditor content={note?.content || ''} onUpdate={handleUpdate} />
<HighlightList bookId={bookId} />
```

- [ ] **步骤 6：手动测试**

1. 打开一本 EPUB 书籍
2. 选中文字 → 工具栏出现（高亮 / 添加到笔记 / 复制）
3. 点击"高亮" → 选择颜色 → 文字在书中被高亮
4. 点击"添加到笔记" → 文字以引用块形式出现在笔记编辑器中，同时被高亮
5. 关闭后重新打开 → 高亮仍然可见
6. 笔记面板下方显示高亮列表

- [ ] **步骤 7：提交**

```bash
git add src/
git commit -m "feat: 文字选中、高亮标注和引用到笔记"
```

---

## 任务 8：样式优化与最终集成

**文件：**
- 修改：`src/index.css`、`src/pages/ReaderPage.tsx` 及多个组件

- [ ] **步骤 1：添加全局样式**

```css
/* src/index.css */
@import "tailwindcss";

body {
  background-color: #111827;
  color: #f3f4f6;
}

.tiptap {
  outline: none;
  min-height: 200px;
}

.tiptap p.is-editor-empty:first-child::before {
  content: attr(data-placeholder);
  color: #6b7280;
  pointer-events: none;
  float: left;
  height: 0;
}

.tiptap blockquote {
  border-left: 3px solid #3b82f6;
  padding-left: 1rem;
  color: #9ca3af;
  margin: 0.5rem 0;
}
```

- [ ] **步骤 2：添加可拖拽调整的面板宽度**

在 ReaderPage 中添加面板宽度状态和拖拽手柄：

```tsx
const [noteWidth, setNoteWidth] = useState(360)
const resizing = useRef(false)

useEffect(() => {
  const handleMouseMove = (e: MouseEvent) => {
    if (!resizing.current) return
    const newWidth = window.innerWidth - e.clientX
    setNoteWidth(Math.max(240, Math.min(600, newWidth)))
  }
  const handleMouseUp = () => { resizing.current = false }
  document.addEventListener('mousemove', handleMouseMove)
  document.addEventListener('mouseup', handleMouseUp)
  return () => {
    document.removeEventListener('mousemove', handleMouseMove)
    document.removeEventListener('mouseup', handleMouseUp)
  }
}, [])

// 在阅读器和笔记面板之间添加拖拽手柄：
{noteOpen && (
  <div
    className="w-1 bg-gray-700 hover:bg-blue-500 cursor-col-resize shrink-0"
    onMouseDown={() => { resizing.current = true }}
  />
)}
```

笔记面板使用 `noteWidth`：`style={{ width: noteWidth }}`

- [ ] **步骤 3：端到端完整测试**

1. 打开应用 `/`
2. 导入 EPUB → 在书库中显示
3. 导入 PDF → 在书库中显示
4. 点击 EPUB → 阅读器打开，目录 + 笔记面板
5. 通过目录导航章节
6. 选中文字 → 高亮 → 高亮列表中可见
7. 选中文字 → 添加到笔记 → 编辑器中出现引用块
8. 手动输入笔记 → 重新打开后仍然存在
9. 切换目录 / 笔记面板
10. 拖拽调整笔记面板宽度
11. 点击 ← 书库 → 返回书库
12. 点击 PDF → 正常渲染，翻页正常
13. 刷新浏览器 → 所有数据持久化

- [ ] **步骤 4：提交**

```bash
git add -A
git commit -m "feat: 样式优化，可调整面板，暗色主题"
```

---

## 总结

| 任务 | 描述 | 关键产出 |
|------|------|----------|
| 1 | 项目脚手架 | Vite + React + Tailwind + Router |
| 2 | 数据库与类型 | Dexie 数据库定义，TypeScript 类型 |
| 3 | 书籍导入与书库 | 拖拽导入，书籍卡片，网格视图 |
| 4 | EPUB 阅读器 | epub.js 渲染，目录导航，进度保存 |
| 5 | PDF 阅读器 | PDF.js 渲染，翻页，进度保存 |
| 6 | 笔记面板 | Tiptap 富文本编辑器，自动保存 |
| 7 | 高亮与引用 | 选中工具栏，多色高亮，引用到笔记 |
| 8 | 样式优化 | 暗色主题，可拖拽面板，端到端测试 |
